[package]
name = "core-agent-app"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "AgentOS product experience and roadmap contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }
